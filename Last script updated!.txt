CREATE DATABASE IF NOT EXISTS ECommerceDB;
USE ECommerceDB;

-- =================== DROP TABLES IN CORRECT ORDER ===================
DROP TABLE IF EXISTS OrderDetails;
DROP TABLE IF EXISTS Orders;
DROP TABLE IF EXISTS Products;
DROP TABLE IF EXISTS Customers;
DROP TABLE IF EXISTS Calendar;
DROP TABLE IF EXISTS Categories;

-- =================== DIMENSION TABLES ===================

CREATE TABLE Categories (
  CategoryID INT AUTO_INCREMENT PRIMARY KEY,
  CategoryName VARCHAR(100) NOT NULL
);

CREATE TABLE Calendar (
  DateID INT AUTO_INCREMENT PRIMARY KEY,
  Date DATE NOT NULL UNIQUE,
  Year INT NOT NULL,
  Month INT NOT NULL,
  Day INT NOT NULL,
  DayOfWeek VARCHAR(10) NOT NULL
);

CREATE TABLE Customers (
  CustomerID INT AUTO_INCREMENT PRIMARY KEY,
  FirstName VARCHAR(50),
  LastName VARCHAR(50),
  Email VARCHAR(100) UNIQUE,
  Phone VARCHAR(15),
  Address VARCHAR(255)
);

CREATE TABLE Products (
  ProductID INT AUTO_INCREMENT PRIMARY KEY,
  ProductName VARCHAR(100),
  Description VARCHAR(255),
  Price DECIMAL(10,2),
  StockQuantity INT,
  CategoryID INT,
  FOREIGN KEY (CategoryID) REFERENCES Categories(CategoryID)
);

-- =================== FACT TABLES ===================

CREATE TABLE Orders (
  OrderID INT AUTO_INCREMENT PRIMARY KEY,
  CustomerID INT NOT NULL,
  DateID INT NOT NULL,
  TotalAmount DECIMAL(10,2),
  FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID),
  FOREIGN KEY (DateID) REFERENCES Calendar(DateID)
);

CREATE TABLE OrderDetails (
  OrderDetailID INT AUTO_INCREMENT PRIMARY KEY,
  OrderID INT NOT NULL,
  ProductID INT NOT NULL,
  Quantity INT,
  UnitPrice DECIMAL(10,2),
  FOREIGN KEY (OrderID) REFERENCES Orders(OrderID),
  FOREIGN KEY (ProductID) REFERENCES Products(ProductID)
);

-- =================== POPULATE DIMENSIONS ===================

INSERT INTO Categories (CategoryName)
SELECT CONCAT('Category ', n)
FROM (SELECT @row := @row + 1 AS n FROM (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4) t1,
                                             (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4) t2,
                                             (SELECT @row := 0) t3 LIMIT 10) AS temp;

-- Calendar (365 Days)
INSERT INTO Calendar (Date, Year, Month, Day, DayOfWeek)
SELECT date_val,
       YEAR(date_val),
       MONTH(date_val),
       DAY(date_val),
       DAYNAME(date_val)
FROM (
  SELECT ADDDATE('2024-01-01', INTERVAL t4.day DAY) AS date_val
  FROM (
    SELECT @d := @d + 1 AS day FROM 
      (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) a,
      (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) b,
      (SELECT @d := -1) init
  ) t4
  WHERE t4.day < 365
) AS dates;

-- Customers (10,000)
INSERT INTO Customers (FirstName, LastName, Email, Phone, Address)
SELECT CONCAT('First', x), CONCAT('Last', x), CONCAT('user', x, '@example.com'),
       CONCAT('010', LPAD(x, 7, '0')), CONCAT(x, ' Main St, City')
FROM (SELECT @x := @x + 1 AS x FROM
      (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) a,
      (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) b,
      (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) c,
      (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) d,
      (SELECT @x := 0) init
      LIMIT 10000) AS temp;

-- Products (100)
INSERT INTO Products (ProductName, Description, Price, StockQuantity, CategoryID)
SELECT CONCAT('Product ', p), CONCAT('Desc for product ', p), ROUND(10 + RAND()*990,2), FLOOR(1 + RAND()*100), FLOOR(1 + RAND()*10)
FROM (
  SELECT @p := @p + 1 AS p FROM
  (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) a,
  (SELECT 0 UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) b,
  (SELECT @p := 0) init
  LIMIT 100) AS temp;

-- =================== INSERT INTO FACTS SAFELY ===================

-- Orders (10,000)
INSERT INTO Orders (CustomerID, DateID, TotalAmount)
SELECT c.CustomerID, d.DateID, ROUND(50 + RAND()*950, 2)
FROM (SELECT CustomerID FROM Customers ORDER BY RAND() LIMIT 10000) c
JOIN (SELECT DateID FROM Calendar ORDER BY RAND() LIMIT 10000) d
LIMIT 10000;

-- OrderDetails (20,000)
INSERT INTO OrderDetails (OrderID, ProductID, Quantity, UnitPrice)
SELECT o.OrderID, p.ProductID, FLOOR(1 + RAND()*5), ROUND(10 + RAND()*990, 2)
FROM (SELECT OrderID FROM Orders ORDER BY RAND() LIMIT 20000) o
JOIN (SELECT ProductID FROM Products ORDER BY RAND() LIMIT 20000) p
LIMIT 20000

